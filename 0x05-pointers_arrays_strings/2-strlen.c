#include "main.h"

/**
 * int _strlen - a function that reyurns the length of a string
 * @s: the string variable
 * @len: the variable that holds the string length
 *
 * Return : len
 */

int _strlen(char *s)
{
len = _strlen(s);

return (len);
}
