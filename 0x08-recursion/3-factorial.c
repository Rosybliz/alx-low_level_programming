#include "main.h"

/**
 * factorial(int n)- a recursive function that returns the
 * factorial of a given number
 * @n: the given number
 * Return: return -1 if n is lower than 0 to indicate error
 */

int factorial(int n)
{
	int s = 0;

	if (n < 0)
	{
		return (-1);
	}
	if (s = n * factorial(n - 1))
		return (s);
	}
}
