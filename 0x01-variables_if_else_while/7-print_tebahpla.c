#include <stdio.h>
/**
 * main - prints lowercase in reverse
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char x = 'z';

		while (x >= 'a')
		{
			putchar(x);
			z--;
		}
	putchar('\n');
	return (0);
}
